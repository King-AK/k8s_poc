apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: k8s-poc-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/k8s-poc-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/k8s-poc-default: "true"
