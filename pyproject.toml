[tool.poetry]
name = "k8s-poc"
version = "0.1.0"
description = ""
authors = ["king-ak <akdavey@email.wm.edu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
